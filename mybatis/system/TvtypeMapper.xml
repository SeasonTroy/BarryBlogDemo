<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bootdang.system.mapper.TvtypeMapper">

    <select id="selectDistinct" resultType="tvtype">
        select * from sys_tvtype  group by title order by tvtype_id
    </select>
</mapper>
